openapi: 3.0.3
info: { title: t, version: '1' }
paths:
  /a:
    get:
      operationId: a
      security: [{ oauth: [read] }, { basic: [] }]
      responses: { '200': { description: ok } }
components:
  securitySchemes:
    oauth: { type: oauth2, flows: {} }
    basic: { type: http, scheme: basic }
